# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
python-dotenv>=1.0.1

# Database — the API reads and writes Supabase (Postgres) for everything.
supabase>=2.6.0

# Risk + eclampsia models. Both are sklearn estimators pickled by backend/ml/,
# and both fall back to rule-based logic when the .pkl is absent.
scikit-learn>=1.3.0

# Needed by backend/ml/ training and data-prep scripts.
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
